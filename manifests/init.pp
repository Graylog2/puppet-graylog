#
# This class does nothing but fail if you try to use it. Please use the
# graylog::server class instead.
#
class graylog {
  fail('Do not use the "graylog" class directly, use the "graylog::server" class!')
}
